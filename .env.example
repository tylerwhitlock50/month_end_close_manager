# Month-End Close Manager - Environment Variables

# Security
SECRET_KEY=change-this-secret-key-in-production
DEBUG=True

# CORS Origins (comma-separated list)
# Add your server IP and any additional origins that need access
# Example: ALLOWED_ORIGINS=http://10.207.24.120:5173,http://your-domain.com
ALLOWED_ORIGINS=http://localhost:5173

# Database (when running outside Docker)
DATABASE_URL=postgresql://monthend_user:monthend_password@localhost:5433/monthend_db

# Redis (when running outside Docker)
REDIS_URL=redis://localhost:6379/0

# Email Configuration (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@monthend.local

# Slack Configuration (optional)
SLACK_BOT_TOKEN=
SLACK_CHANNEL=#finance-close

# File Storage
FILE_STORAGE_PATH=./files
MAX_FILE_SIZE_MB=50

# Timezone
TZ=America/New_York
